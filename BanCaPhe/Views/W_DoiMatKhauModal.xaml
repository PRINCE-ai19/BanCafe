<Window x:Class="BanCaPhe.Views.W_DoiMatKhauModal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:BanCaPhe.ViewModel"
        xmlns:conv="clr-namespace:BanCaPhe.Converters"
        Title="Đổi mật khẩu"
        Height="250"
        Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Window.Resources>
        <conv:MultiValueConverter x:Key="MultiValueConverter"/>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- THÔNG TIN NHÂN VIÊN -->
        <TextBlock Grid.Row="0" 
                   Text="{Binding HoTen, StringFormat='Đổi mật khẩu cho: {0}'}"
                   Foreground="{StaticResource OnSurfaceBrush}"
                   FontWeight="Bold"
                   Margin="0,0,0,15"/>

        <!-- MẬT KHẨU MỚI -->
        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Text="Mật khẩu mới" Foreground="{StaticResource OnSurfaceBrush}"/>
            <PasswordBox x:Name="txtMatKhauMoi" 
                         Padding="12,10"
                         Height="40"/>
        </StackPanel>

        <!-- XÁC NHẬN MẬT KHẨU -->
        <StackPanel Grid.Row="2" Margin="0,0,0,10">
            <TextBlock Text="Xác nhận mật khẩu" Foreground="{StaticResource OnSurfaceBrush}"/>
            <PasswordBox x:Name="txtXacNhan" 
                         Padding="12,10"
                         Height="40"/>
        </StackPanel>

        <!-- BUTTONS -->
        <StackPanel Grid.Row="4"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,15,0,0">
            <Button Content="Đổi mật khẩu"
                    Width="110"
                    Margin="0,0,8,0"
                    Style="{StaticResource PrimaryButtonStyle}">
                <Button.Command>
                    <Binding Path="DoiMatKhauCommand"/>
                </Button.Command>
                <Button.CommandParameter>
                    <MultiBinding Converter="{StaticResource MultiValueConverter}">
                        <Binding ElementName="txtMatKhauMoi"/>
                        <Binding ElementName="txtXacNhan"/>
                    </MultiBinding>
                </Button.CommandParameter>
            </Button>
            <Button Content="Hủy"
                    Width="90"
                    Style="{StaticResource SecondaryButtonStyle}"
                    Command="{Binding HuyCommand}"/>
        </StackPanel>
    </Grid>
</Window>
