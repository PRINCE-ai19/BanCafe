<Window x:Class="BanCaPhe.Views.ThanhToanWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BanCaPhe.Views"
        mc:Ignorable="d"
       WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">
    <Border CornerRadius="12"
            Background="{StaticResource SurfaceContainerLowestBrush}"
            Padding="20"
            Width="320">

        <StackPanel>

            <!-- TITLE -->
            <TextBlock Text="Chọn phương thức thanh toán"
                       FontSize="18"
                       FontWeight="Bold"
                       Margin="0,0,0,15"
                       TextAlignment="Center"
                       Foreground="{StaticResource OnSurfaceBrush}"/>

            <!-- TIỀN MẶT -->
            <Button Content="💵 Tiền mặt"
                    Height="45"
                    Margin="0,5"
                    Style="{StaticResource PrimaryButtonStyle}"
                    Command="{Binding TienMatCommand}"/>

            <!-- CHUYỂN KHOẢN -->
            <Button Content="💳 Chuyển khoản"
                    Height="45"
                    Margin="0,5"
                    Style="{StaticResource PrimaryButtonStyle}"
                    Command="{Binding ChuyenKhoanCommand}"/>

            <!-- VNPAY -->
            <Button Content="📱 VNPay QR"
                    Height="45"
                    Margin="0,5"
                    Style="{StaticResource PrimaryButtonStyle}"
                    Command="{Binding VnpayCommand}"/>

            <!-- HỦY -->
            <Button Content="Hủy"
                    Height="35"
                    Margin="0,15,0,0"
                    Style="{StaticResource SecondaryButtonStyle}"
                    Command="{Binding DongCommand}"/>

        </StackPanel>
    </Border>
</Window>
